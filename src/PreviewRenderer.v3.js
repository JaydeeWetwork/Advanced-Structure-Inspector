/**
 * @deprecated Import `./PreviewRenderer.js` instead.
 * Kept as a re-export so any old ?v= cache entries resolve to the single renderer.
 */
export { default } from "./PreviewRenderer.js?v=judo19";
