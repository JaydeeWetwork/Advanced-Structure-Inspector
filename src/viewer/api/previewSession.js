/**
 * Public Preview Session API — park / restore / LRU WebGL contexts.
 * Lives in app/ (session ownership), re-exported here for a stable import path.
 */

export { default as PreviewSessionManager } from "../../app/PreviewSessionManager.js";
